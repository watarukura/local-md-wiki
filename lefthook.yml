pre-commit:
  parallel: true
  jobs:
    - name: oxlint
      run: pnpm oxlint
      glob: "*.{js,ts,jsx,tsx}"
    - name: oxfmt
      run: pnpm fmt
      glob: "*.{js,ts,jsx,tsx}"
    - name: test
      run: make test
      glob: "*.go"
    - name: go fmt
      run: make format
      glob: "*.go"
    - name: go lint
      run: make lint
      glob: "*.go"
    - name: actionlint
      glob: ".github/workflows/*.yam?l"
      run: actionlint
    - name: gitleaks
      run: gitleaks detect -v
    - name: typos
      run: typos .
